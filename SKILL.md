---
name: git-ship
description: |
  自动化 Git 工作流一键交付助手：检查当前改动，基于目标仓库默认分支创建或复用任务分支，
  完成本地验证、commit、push、PR、CI 修复、合并和分支清理。默认 `ship` 会持续推进到实际合并；
  明确使用 `ship --review` 时，在创建或更新 PR 后停止。调用 ship 本身就是对本次完整流程的授权，
  但不覆盖认证、权限、人工审批、仓库保护和无法判断的任务边界。

  仅在用户明确表达 ship 意图时触发，例如：
  - 直接说「ship」「/ship」「git ship」
  - 明确要求完整走完 git 流程：「拉 main、切分支、提 PR、合并」
  - 明确要求把当前改动发布并合并

  不应触发的情况：
  - 只是想 commit、push 或创建 PR
  - 讨论 Git 概念、解释流程或 review 方案
  - 没有明确授权完整发布流程
---

# git-ship — 自主 Git 交付

## 模式与成功标准

`ship` 的默认结果是改动已经进入目标仓库默认分支：

```text
工作区改动 → 识别范围 → 最新默认分支 → 任务分支 → commit → 本地验证
→ push → PR → CI 修复循环 → 合并 → 清理 → 回到默认分支
```

明确使用 `ship --review` 时，流程在创建或更新 PR 后停止：

```text
工作区改动 → 识别范围 → 最新默认分支 → 任务分支 → commit → 本地验证
→ push → PR（停止）
```

两种模式都不等待用户确认自动推断的分支名、commit message、PR 标题或 PR 正文。
`--review` 是唯一改变交付终点的模式开关；不带该开关的 `ship` 必须继续推进到合并。

默认 `ship` 只有同时满足以下条件才算完成：

1. PR 已实际合并到目标仓库默认分支；创建 PR 或开启自动合并不算完成。
2. 必需检查已经通过，或仓库没有可运行的必需检查。
3. 本地已回到默认分支，并在可以安全 fast-forward 时同步到合并后的提交。
4. 本次运行创建的任务分支已清理；预先存在的分支不因本次运行被删除。

`ship --review` 的完成标准是 PR 已创建或更新并返回 URL；它不等待或修复 CI，不合并 PR，
也不删除远程或本地分支。之后再次执行不带 `--review` 的 `ship` 可以继续完整交付。

## 共享边界：先识别，再写入

开始前读取有效的 `AGENTS.md`、仓库文档和 Git 状态，至少记录：

```bash
git status --short --branch
git branch --show-current
git diff --name-only
git diff --cached --name-only
git diff --stat
git log --oneline --decorate -n 10
git remote -v
gh auth status
```

同时确定目标仓库的真实默认分支，不把 `main` 写死：优先使用 GitHub 仓库元数据或
`origin/HEAD`，下文的 `<base>` 代表该分支。

在第一次 stash、切分支或提交前，建立本次任务的文件范围：

- 当前工作区的已修改、已暂存和未跟踪文件必须能根据用户请求、调用链和项目规则明确归入本次任务。
- 如果存在无法区分的既有改动、混合未跟踪文件、未授权的预先提交，停止并保留原现场；不要为了继续而猜测归属。
- 范围明确时记录显式文件列表；后续只对这些路径 `git add`，绝不使用 `git add -A` 或无范围的 `git add .`。
- 不覆盖、删除、重置或改写范围外的改动和提交。不要使用 `git reset --hard`、force push、跳过钩子或跳过有效检查。

这一步是默认全自动流程中的边界识别，不是把正常的可修复失败交还给用户。

## 第 0 步：确定交付上下文

自动确定以下信息：

**分支名**

- 用户已提供时直接使用。
- 否则根据改动内容和仓库既有前缀推断 `<type>/<short-desc>`。
- 当前分支已有明确属于本次任务的待发布提交时复用它，并保留其历史；否则从最新 `origin/<base>` 创建唯一任务分支。
- 本地或远端同名分支属于其他功能时换用唯一后缀；同名分支确认属于同一功能时复用并先 fetch。

**提交和 PR 文案**

- 用户已提供时直接使用。
- 否则根据实际 diff 生成 Conventional Commit、PR 标题和正文。
- PR 正文包含 2–3 条 Summary、改动文件概览、验证结果和 `🤖 Shipped via git-ship`。

向用户简短说明将使用的分支和 commit 后立即继续，不等待确认。

没有未提交改动时，检查当前分支相对 `origin/<base>` 是否存在明确属于本次任务的待发布提交；两者都没有才报告没有可发布内容并停止。

## 第 1 步：同步默认分支并保留范围内改动

只有范围已经明确且没有外部阻塞时，才执行以下动作：

1. 记录原分支、原 HEAD、工作区状态、已有 stash 和本次显式文件列表。
2. 仅在需要切分支且存在工作区改动时，创建带唯一标识的 stash；stash 只包含本次已确认范围，并记录引用。
3. `git fetch --prune origin`，以最新 `origin/<base>` 为基准。
4. 当前分支已有本次任务提交时保留该分支并合并最新 `origin/<base>`；没有待发布提交时从最新 `origin/<base>` 新建任务分支。
5. 恢复本次 stash；如果产生冲突，按冲突流程处理。确认恢复内容无误并提交后，才删除本次创建的 stash，不触碰用户已有 stash。

如果本地 `<base>` 有独有提交而不能 fast-forward，不删除或覆盖它们；发布分支仍可从最新远端默认分支继续，最终说明本地默认分支的差异。

## 第 2 步：提交范围内改动

恢复改动后逐个暂存已记录的路径：

```bash
git add -- <scoped-path-1> <scoped-path-2>
git diff --cached --check
git diff --cached --stat
git commit -m "<commit-message>"
```

提交前再次确认暂存区没有范围外文件。提交钩子失败属于验证失败：修复根因、重新暂存并再次提交，
不要使用 `--no-verify`。首次 push 前的修复可以继续整理在当前提交中；首次 push 后的修复创建普通新提交，
不要为整理历史而 amend、rebase 或 force push。

## 第 3 步：本地验证与修复循环

按项目有效规则寻找可信命令：

1. 优先读取 `AGENTS.md`、`CONTRIBUTING.md`、`README.md` 和 CI 配置。
2. 再检查 `package.json`、`pyproject.toml`、`Makefile` 等项目入口及锁文件。
3. 先运行与改动直接相关的检查，再运行仓库要求的 lint、typecheck、test 或 build。
4. 找不到可信命令时明确报告跳过的验证；不要编造命令或把没有检查说成已通过。

验证失败时持续处理：保留完整错误，定位第一个根因，做范围内最小正确修复，先重跑失败的最小检查，
再重跑完整必需检查。不得删除或弱化有效断言、关闭规则或跳过检查。

如果失败来自范围外的既有问题、缺少无法取得的权限/密钥、平台故障，或修复后同一失败再次出现且没有新证据，
停止并保留现场。只要每轮都有新证据或实际进展，就不要把耗时或多轮调试当作阻塞。

## 第 4 步：推送和 PR

正常推送任务分支：

```bash
git push -u origin <branch-name>
```

推送被拒绝时先分类：

- 远端同名分支属于其他功能：换唯一分支名，不覆盖它。
- 远端同名分支属于本次任务：fetch、合并并验证后正常 push。
- 网络或 GitHub 临时失败：短暂重试并重新检查状态。
- 认证、写权限或保护规则不足：保留现场并请求唯一必要的外部动作。

使用 `gh pr create` 创建 PR；已有本次任务 PR 时复用并更新，不重复创建。base 使用真实 `<base>`，
head 使用任务分支。创建或更新后展示 PR URL。

如果是 `ship --review`，此处立即返回并结束本次运行：不等待 CI、不修复 CI、不合并、不删除分支。

## 第 5 步：等待 CI 并修复

只有默认 `ship` 进入本阶段。使用仓库可用命令等待必需检查，例如：

```bash
gh pr checks <pr-number> --watch
```

检查失败时读取 `gh pr checks`、`gh run view --log-failed` 等可用日志；能在本地复现且属于本次范围时修复、验证、
创建新提交并正常 push，然后重新等待。持续到所有必需检查通过，或遇到明确外部阻塞。

长期 pending 时检查 workflow、队列和 GitHub 状态；能安全重跑任务时可以重跑。不得使用管理员合并、跳过检查、
绕过分支保护或伪造通过状态。

## 第 6 步：同步、合并和清理

合并前再次 fetch 最新 `<base>`，把它合并到任务分支；如有冲突，按冲突流程解决，重新本地验证、push 并等待新一轮 CI。

检查 PR 状态、必需检查、分支保护和必需审批后，按目标仓库既有 merge 策略合并。没有明确仓库偏好时使用仓库已允许且
与现有 PR 习惯一致的方式；不得绕过保护或使用 `--admin`。如果需要人工审批、签名或特殊权限，停止并说明准确返回信息。

只有任务分支由本次运行创建并已成功合并时，才请求删除远程分支并删除对应本地分支/工作树。预先存在的分支保留并报告。

## 第 7 步：确认完成

```bash
git switch <base>
git pull --ff-only origin <base>
git status --short --branch
gh pr view <pr-number> --json state,mergedAt,mergeCommit,url
```

确认 PR 状态为 `MERGED`，打印最终默认分支提交 hash、PR URL、验证和清理结果。若本地默认分支有预先存在的独有提交而不能
fast-forward，保留它们，不做破坏性处理，同时明确远端合并已完成和本地差异。

## 冲突流程

适用于 stash apply、合并最新默认分支、远端同名分支和合并前更新：

1. 查看 `git status`、未解决文件和左右提交关系。
2. 阅读冲突文件的 base、当前分支、对方版本及相关调用方、测试、类型和配置。
3. 保留最新线上行为和本次功能目标；两边都有效时整合接口、数据流和边界条件。
4. 清除全部冲突标记，`git add` 仅限已确认范围，先做最小验证，再做完整验证。
5. 创建普通合并或修复提交，继续原流程。

两种行为互斥且产品意图仍无法判断时才询问用户，并保留现场；不能用盲选 `ours`/`theirs`、reset 或丢弃提交消除冲突。

## 停止条件与安全底线

- `ship` 的明确调用授权当前任务范围内的 commit、正常 push、PR、CI 修复、合并和清理；不授权范围外改动。
- 普通 `commit`、`push`、`创建 PR` 请求不触发完整 Ship。
- 不使用 force push、`git reset --hard`、`git clean`、跳过钩子/测试、管理员合并或绕过分支保护。
- 不泄露密钥，不把密钥、无关改动或失败检查一起发布。
- 只有认证/权限、必需人工审批、持续外部故障、范围歧义、不可安全修复的冲突或重复失败才暂停。
- 暂停时说明已完成步骤、准确阻塞信息和用户只需完成的最小动作；保留可继续的分支、提交和日志。
